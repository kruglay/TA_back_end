<% layout('../layouts/page') %>
<% block('title', 'USERS') %>
<ul>
    <% posts.forEach(function(post, i){ %>
    <li>
        <span> <%= post.title %> <%- post.text %> </span>
    </li>
    <%});%>
</ul>

